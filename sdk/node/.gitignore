node_modules
dist
target

# napi build outputs (generated by `napi build`)
*.node
# binding.js / binding.d.ts are committed: the fat-package publish ships the napi
# loader from the repo (the publish runner doesn't run `napi build`).

.DS_Store
*.log

# Bundled native assets (regenerated by bundle:assets)
native
